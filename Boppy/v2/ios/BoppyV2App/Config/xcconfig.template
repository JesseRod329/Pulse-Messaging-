// Copy this to a local xcconfig and set real values before production runs.
// NOTE: xcconfig treats // as a comment, so URLs must use $(SLASH) escape.
SLASH = /
SUPABASE_URL = https:$(SLASH)/YOUR-PROJECT.supabase.co
SUPABASE_ANON_KEY = YOUR_SUPABASE_ANON_KEY
SUPABASE_EDGE_BASE_URL = https:$(SLASH)/YOUR-PROJECT.functions.supabase.co
MAPBOX_ACCESS_TOKEN = YOUR_MAPBOX_TOKEN
