# Stitch Design Source

The `stitch/` directory is raw design export input.

App-ready files must be promoted to iOS source folders after cleanup and conversion.

Do not import directly from raw export paths in production code.
